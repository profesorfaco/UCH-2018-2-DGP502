/*
  El comentario de varias líneas
  se escribe con slash - asterisco - asterisco - slash

  Atajos recomendados:

  [Comando] + [Shift] + [F] = Referencias del lenguaje
  [Comando] + [T] = Ordena todo el texto escrito
  [Comando] + [R] = Corre (run) una compilación del programa
  [Comando] + [U] = Carga (upload) el programa al Arduino
  [Comando] + [-] = Convierte línea(s) en comentario

*/

void setup() {
  /*
    Como en Processing
    aquí tenemos un setup
    lo que aquí se describa
    se ejecutará una vez
  */
}

void loop() {
  /*
    A diferencia de Processing
    aquí no se dibuja (draw)
    sino que prepara un ciclo (loop)
    que controlará una placa Arduino
    una vez se cargue el código en ella
    (ver Herramientas > Gestor de tarjetas)
  */
}
